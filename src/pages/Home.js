import {React} from "react";
import Navbar from "../components/Navbar";

const Home = ({isLoggedIn}) => {
    return(
        <div>
            
            <div>
                This is home page
            </div>
        </div>
    )
}

export default Home;